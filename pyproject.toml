[project]
name = "building-a-brain"
version = "0.1.0"
description = ""
authors = [
    {name = "Lcs-ns",email = "lucas.nas314@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
ipykernel = "^6.29.5"

